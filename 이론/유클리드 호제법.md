# 유클리드 호제법

```python
# 24와 18의 최대 공약수, 최소 공배수 구하기
# (24, 18, 6) => a, b, 나머지
# (18, 6, 0) => 나머지가 0이 되면 최대 공약수 b
# 최소 공배수 = ((a*b)/최대 공약수)

n, m = max(24, 18), min(24, 18)

while m > 0:	# 나머지가 0이 되면 n은 최대 공약수
    n, m = m, n%m
    
print(n)	# 최대 공약수
print((n*m)//n)		# 최소 공배수
```

